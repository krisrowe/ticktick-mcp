service:
  entrypoint: ticktick.mcp.server:mcp_app
  mcp_path: /mcp
  auth: bearer
  runtime: v0.2.0
prerequisites:
  secrets:
    TICKTICK_ACCESS_TOKEN:
      description: TickTick OAuth access token for API calls
